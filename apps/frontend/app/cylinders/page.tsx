"use client";

import { useState } from "react";
import { Download, MapPin, Plus, Wrench } from "lucide-react";
import { GasRing } from "@/components/gas-ring";
import { Modal, PageHeading } from "@/components/ui-kit";
import { cylinders as initial } from "@/lib/demo-data";

export default function CylindersPage() {
  const [items, setItems] = useState(initial);
  const [open, setOpen] = useState(false);
  const [serial, setSerial] = useState("");
  const add = () => { if (!serial.trim()) return; setItems([...items, { id: Date.now(), serial, capacity: "12 kg", gas: 100, status: "ACTIVE", installed: new Date().toLocaleDateString() }]); setSerial(""); setOpen(false); };
  return <div className="mx-auto max-w-[1180px]"><PageHeading title="Cylinder Management" subtitle="Track and manage household LPG cylinders." action={<button className="btn-secondary"><Download size={15} /> Export Data</button>} /><div className="grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><section className="card p-5"><div className="mb-5 flex items-center justify-between"><span className="text-[11px] font-extrabold uppercase tracking-wide text-slate-500">Cylinder #001</span><span className="badge badge-green">Active</span></div><div className="flex flex-col items-center"><GasRing value={68} size={170} /><button onClick={() => setOpen(true)} className="btn-primary mt-3"><Plus size={15} /> Add Cylinder</button></div><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded border border-slate-200 bg-slate-50 p-3"><div className="text-[10px] font-bold uppercase text-slate-500">Pressure</div><div className="mt-1 text-[17px] font-extrabold">2.2 bar</div></div><div className="rounded border border-slate-200 bg-slate-50 p-3"><div className="text-[10px] font-bold uppercase text-slate-500">Remaining</div><div className="mt-1 text-[17px] font-extrabold">9.4 kg</div></div></div><button className="btn-secondary mt-4 w-full">View Cylinder Metrics</button></section><aside className="rounded-lg bg-[#073b82] p-5 text-white"><div className="badge bg-blue-200 text-[#073b82]">SHELL UGANDA</div><h2 className="mt-4 text-[18px] font-extrabold">Directions</h2><p className="mt-1 text-[11px] text-blue-100">LPG pickup station · Ntinda</p><div className="relative my-5 h-36 overflow-hidden rounded bg-[#dbe7d9]"><div className="absolute inset-0 opacity-70" style={{ backgroundImage: "linear-gradient(25deg, transparent 45%, #8faf8b 46%, #8faf8b 49%, transparent 50%), linear-gradient(115deg, transparent 42%, #b5c9df 43%, #b5c9df 47%, transparent 48%)", backgroundSize: "55px 45px" }} /><MapPin className="absolute left-1/2 top-1/2 text-red-600" /></div><button className="btn-secondary w-full">Request Refill</button></aside></div><section className="card mt-5 overflow-hidden"><div className="flex items-center justify-between border-b border-slate-200 p-4"><h2 className="section-title">Refill Log</h2><button className="btn-secondary"><Wrench size={14} /> All Actions</button></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-[11px]"><thead className="bg-slate-50 text-[10px] uppercase text-slate-500"><tr>{["Date & Time", "Cylinder ID", "Action", "Status", "Operator", "Details"].map(x => <th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody>{items.map((item, i) => <tr key={item.id} className="border-t border-slate-200"><td className="px-4 py-4">{item.installed}</td><td className="px-4 font-bold text-[#073b82]">{item.serial}</td><td className="px-4">{i === 0 ? "Refill Completed" : "Cylinder Added"}</td><td className="px-4"><span className={item.status === "ACTIVE" ? "badge badge-green" : "badge badge-red"}>{item.status}</span></td><td className="px-4">Guardian Network</td><td className="px-4 font-bold text-[#073b82]">View</td></tr>)}</tbody></table></div></section><Modal open={open} title="Add Cylinder" onClose={() => setOpen(false)}><label className="label">Serial number</label><input className="field" value={serial} onChange={e => setSerial(e.target.value)} placeholder="CYL-0003" /><div className="mt-5 flex justify-end gap-2"><button className="btn-secondary" onClick={() => setOpen(false)}>Cancel</button><button className="btn-primary" onClick={add}>Add Cylinder</button></div></Modal></div>;
}
